---
order: 3
zh-CN:
	title: 动态改变主题
  prompt: '主题已变更'
en-US:
	title: Dynamic Change Theme
  prompt: 'theme changed' 
---

```jsx
import { ColorPicker, ThemeSdk, Notify, ThemeScene } from 'zent';
import { useState } from 'react';

const { generateTheme, applyTheme } = ThemeSdk;

const colors = [
  '#ed6a18',
  '#ed9f18',
  '#edd418',
  '#bad415',
  '#5bd415',
  '#15d48e',
  '#15bad4',
  '#155bd4',
  '#412ad4',
  '#8e15d4',
  '#d415ba',
  '#d42f15',
];

const ThemeColors = () => {
  const colorScene = [
    ThemeScene.DefaultHoverBackgroundColor,
    ThemeScene.PrimaryHoverBackgroundColor,
    ThemeScene.PrimaryBackgroundColor,
    ThemeScene.PrimaryActiveBackgroundColor
  ];

	const [color, setColor] = useState('#155bd4');

  const onChangeComplete = hex => {
		setColor(hex);
    const theme = generateTheme({ colors: [{ baseColor: hex, scene: colorScene }] });
    applyTheme(theme);

		Notify.success('{i18n.prompt}');
  };

  return (
    <>
      <ColorPicker color={color} type="simple" onChange={onChangeComplete} presetColors={colors} />
      <span className="deme-theme-base-color">{color}</span>
    </>
	)
}

ReactDOM.render(
	<ThemeColors />,
	mountNode
);
```
